\* TLC configuration for the committed instance (correct design).
\* Run: java -cp tla2tools.jar tlc2.TLC -config CXL_EBFI_Invariant.cfg CXL-EBFI_Invariant.tla
\* Expected: no invariant violation -- EBFI (counter-freshness vs. the trusted
\* version anchor) + ForwardIntegrity hold over ALL reachable states for these
\* bounds (lastAccept is a fixed-size function, so the state space is finite and
\* fully enumerated -- this is verification, not sampling).
\*
\* To reproduce the same-epoch-rollback counterexample that the prior (broken)
\* epoch-only rule admits, set StrictCounter <- FALSE below and re-run; TLC
\* reports an Inv violation (the formal analogue of the implementation bug).
SPECIFICATION Spec
INVARIANT Inv
CONSTANTS
  Hosts = {1,2}
  Addrs = {1,2}
  MaxEpoch = 3
  MaxCtr = 2
  RetentionW = 2
  StrictCounter = TRUE
